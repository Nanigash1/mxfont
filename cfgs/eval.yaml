dset:
  test:
    data_dir: mxfont/data/images/test   # path to reference images
    source_font: mxfont/data/ttfs/val/arial.ttf    # path to .ttf file used as the source font during the generation
    gen_chars_file: mxfont/data/kz_gen.json   # path to file of the characters to generate. Leave blank if you want to generate all the available characters in the source font.
